"""
Package: contracts
Handles data contract management — loading, validation, and registry.

Modules:
    - contract_loader: Load contract definitions from JSON files
    - contract_validator: Validate datasets against contracts
    - schema_validator: Validate data schemas (field presence, types)
    - contract_registry: Central registry of available contracts and versions
"""
